'use strict';

describe('myApp.logout module', function() {

  beforeEach(module('myApp.logout'));

  describe('logout controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var logoutCtrl = $controller('LogoutCtrl');
      expect(logoutCtrl).toBeDefined();
    }));

  });
});